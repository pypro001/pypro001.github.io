<code>
#include<iostream>
using namespace std;
void swap(int a , int b)
{
int temp = a;
a = b;
b = temp;
cout<<"INSIDE THE SWAP FUNCTION = "<<endl;
cout<<"a = "<<a<<" and "<<"b = "<<b<<endl;
}
void swapByReference(int &a , int &b)
{
int temp = a;
a = b;
b = temp;
}
void swapByPointer(int *a , int *b)
{
int temp = *a;
*a = *b;
*b = temp;
}
int main()
{
int a , b;
cout<<"ENTER INTEGER FOR SWAPPING = ";
cin>>a>>b;
cout<<"BEFORE SWAP NUMBERS ARE = "<<endl;
cout<<"a = "<<a<<" and "<<"b = "<<b<<endl<<endl;
swap(a,b); // pass by value ,
// will not swap bcoz in function values are being copied in new
varibales in fun defination,
// will swaped only in function
cout<<"OUTSIDE THE SWAP FUNCTION = "<<endl;
cout<<"a = "<<a<<" and "<<"b = "<<b<<endl<<endl;
cout<<"PASS BY REFRENCE "<<endl; // pass by refrence
swapByReference(a,b);
cout<<"a = "<<a<<" and "<<"b = "<<b<<endl<<endl;
cout<<"PASS BY POINTER"<<endl; // pass by pointer
swapByPointer(&a, &b);
cout<<"a = "<<a<<" and "<<"b = "<<b<<endl<<endl;
return 0;
}
</code>
