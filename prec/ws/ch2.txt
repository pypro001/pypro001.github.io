<html>

    <code>

       // prec 2
       ||Write a program to perform for 2D Rotation and 2D Reflection.||
       2D Rotation
Program code :-
#include<stdio.h>
#include<graphics.h>
#include<conio.h>
#include<math.h>
void main()
{
int gd=DETECT,gm;
int x1,y1,x2,y2;
float b1,b2;
float t,deg;
initgraph(&gd,&gm,"C://TURBOC3//BGI");
printf("enter the coordinate og line/n");
scanf("%d%d%d%d",&x1,&y1,&x2,&y2);
setcolor(6);
line(x1,y1,x2,y2);
getch();
printf("enter the angle of rotation:");
scanf("%f",&deg);
t=(22*deg)/(180*7);
b1=abs((x2*cos(t))-(y2*sin(t)));
b2=abs((x2*sin(t))+(y2*cos(t)));
line(x1,y1,b1,b2);
getch();
closegraph();
}

2D Reflection.
Program Code :-
#include<stdio.h>
#include<conio.h>
#include<graphics.h>
#include<stdlib.h>
void main()
{
int gd=DETECT,gm=DETECT,midx,midy,x1,y1,x2,y2;
initgraph(&gd,&gm,"C://TURBOC3//BGI");
midx=getmaxx()/2;
midy=getmaxy()/2;
line(0,midy,midx*2,midy);
line(midx,0,midx,midy*2);
printf("Enter the co ordinate for P1: ");
scanf("%d%d",&x1,&y1);
printf("Enter the co ordinate for P2: ");
scanf("%d%d",&x2,&y2);
line(midx+x1,midy-y1,midx+x2,midy-y2);
line(midx+x1,midy+y1,midx+x2,midy+y2);
line(midx-x1,midy-y1,midx-x2,midy-y2);
line(midx-x1,midy+y1,midx-x2,midy+y2);
getch();
closegraph();
}


    </code>

</html>
